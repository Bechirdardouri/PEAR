# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Virtual environments
.venv/
venv/
env/
.python-version

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Model and dataset caches (committed: only the parquet outputs)
.hf_cache/
.cache/
*.bin
*.safetensors
*.pt
*.pth

# Generated artefacts that the user may regenerate (we DO commit the
# canonical results/* so the repo is self-contained; uncomment to skip)
# results/figures/*.png
# results/figures/*.pdf

# Local logs (we keep one example per stage under results/, not the raw
# per-row probe logs from a one-off run)
*.log
!scripts/*.log

# Distribution / packaging
build/
dist/
*.whl

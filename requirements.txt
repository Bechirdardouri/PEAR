# Minimal runtime dependencies. For development extras (pytest, ruff)
# see pyproject.toml [project.optional-dependencies].
#
# Tested under:
#   torch         2.7.0+cu126
#   transformers  5.12.0
#   Python        3.12
#   single H100 80 GB (PCIe), bf16
#
# flash-attention is auto-detected at model load if installed; otherwise
# the torch SDPA path is used.

torch>=2.4
transformers>=4.50
accelerate>=0.30
pandas>=2.2
pyarrow>=15.0
numpy>=1.26
scipy>=1.13
Pillow>=10.0
datasets>=2.20
tqdm>=4.66
matplotlib>=3.9
python-Levenshtein>=0.25
